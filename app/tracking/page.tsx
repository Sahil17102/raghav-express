'use client';
import { useState } from 'react';
import { CheckCircle2, Circle, MapPin, Package, Search, Truck } from 'lucide-react';
import { SiteFooter, SiteNav } from '../site-chrome';
const stages=['Booked','Pickup confirmed','In transit','Out for delivery','Delivered'];
export default function Tracking(){const [id,setId]=useState('');const [show,setShow]=useState(false);return <><SiteNav/><section className="inner-hero tracking-hero"><p className="eyebrow"><span/> SHIPMENT TRACKING</p><h1>Follow every<br/><i>important mile.</i></h1><p>Enter your consignment number to request the latest shipment update from Raghav Express.</p><form className="hero-track" onSubmit={e=>{e.preventDefault();setShow(true)}}><input value={id} onChange={e=>setId(e.target.value)} placeholder="Enter consignment number"/><button><Search size={18}/> Track shipment</button></form></section><section className="tracking-picture"><img src="/bright-logistics-hub.png" alt="Courier shipment being prepared at a bright logistics hub"/><div><p className="eyebrow dark-eyebrow"><span/> LIVE HANDOFFS</p><h2>Scanned, sorted<br/>and on the move.</h2><p>From pickup scanning to destination delivery, each major handoff is managed with care.</p></div></section><section className="tracking-result section-wrap">{show?<><div className="shipment-head"><span><Package/> RAGHAV SHIPMENT</span><b>{id || 'RX123456789'}</b><em>In transit</em></div><div className="timeline">{stages.map((s,i)=><div className={i<3?'done':''} key={s}>{i<3?<CheckCircle2/>:<Circle/>}<span>{s}<small>{i===2?'Shipment is moving to the destination hub':i<2?'Completed':'Awaiting update'}</small></span></div>)}</div><div className="shipment-meta"><span><MapPin/> Jodhpur, Rajasthan</span><span><Truck/> Destination dispatch in progress</span></div></>:<div className="empty-track"><Truck size={42}/><h2>Ready when you are.</h2><p>Use your consignment number above to see a simple shipment journey.</p></div>}</section><SiteFooter/></>}
